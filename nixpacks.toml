[phases.setup]
nixPkgs = ["nodejs", "python311", "ffmpeg"]

[phases.install]
cmds = [
  "npm ci",
  "python -m pip install --upgrade pip",
  "python -m pip install yt-dlp"
]

[start]
cmd = "npm start"